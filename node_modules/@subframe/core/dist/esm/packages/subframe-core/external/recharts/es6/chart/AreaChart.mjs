import { generateCategoricalChart } from './generateCategoricalChart.mjs';
import { Area } from '../cartesian/Area.mjs';
import { XAxis } from '../cartesian/XAxis.mjs';
import { YAxis } from '../cartesian/YAxis.mjs';
import { formatAxisMap } from '../util/CartesianUtils.mjs';

/**
 * @fileOverview Area Chart
 */
var AreaChart = generateCategoricalChart({
  chartName: 'AreaChart',
  GraphicalChild: Area,
  axisComponents: [{
    axisType: 'xAxis',
    AxisComp: XAxis
  }, {
    axisType: 'yAxis',
    AxisComp: YAxis
  }],
  formatAxisMap: formatAxisMap
});

export { AreaChart };
