import { generateCategoricalChart } from './generateCategoricalChart.mjs';
import { PolarAngleAxis } from '../polar/PolarAngleAxis.mjs';
import { PolarRadiusAxis } from '../polar/PolarRadiusAxis.mjs';
import { formatAxisMap } from '../util/PolarUtils.mjs';
import { Pie } from '../polar/Pie.mjs';

/**
 * @fileOverview Pie Chart
 */
var PieChart = generateCategoricalChart({
  chartName: 'PieChart',
  GraphicalChild: Pie,
  validateTooltipEventTypes: ['item'],
  defaultTooltipEventType: 'item',
  legendContent: 'children',
  axisComponents: [{
    axisType: 'angleAxis',
    AxisComp: PolarAngleAxis
  }, {
    axisType: 'radiusAxis',
    AxisComp: PolarRadiusAxis
  }],
  formatAxisMap: formatAxisMap,
  defaultProps: {
    layout: 'centric',
    startAngle: 0,
    endAngle: 360,
    cx: '50%',
    cy: '50%',
    innerRadius: 0,
    outerRadius: '80%'
  }
});

export { PieChart };
