"use client";
import * as RadixPopover from '@radix-ui/react-popover';

var Anchor = RadixPopover.Anchor;
var Arrow = RadixPopover.Arrow;
var Close = RadixPopover.Close;
var Content = RadixPopover.Content;
var Popover = RadixPopover.Popover;
var PopoverAnchor = RadixPopover.PopoverAnchor;
var PopoverArrow = RadixPopover.PopoverArrow;
var PopoverClose = RadixPopover.PopoverClose;
var PopoverContent = RadixPopover.PopoverContent;
var PopoverPortal = RadixPopover.PopoverPortal;
var PopoverTrigger = RadixPopover.PopoverTrigger;
var Portal = RadixPopover.Portal;
var Root = RadixPopover.Root;
var Trigger = RadixPopover.Trigger;

export { Anchor, Arrow, Close, Content, Popover, PopoverAnchor, PopoverArrow, PopoverClose, PopoverContent, PopoverPortal, PopoverTrigger, Portal, Root, Trigger };
