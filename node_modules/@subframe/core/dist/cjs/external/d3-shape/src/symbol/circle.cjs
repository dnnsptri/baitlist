'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var math = require('../math.cjs');

var symbolCircle = {
  draw(context, size) {
    const r = math.sqrt(size / math.pi);
    context.moveTo(r, 0);
    context.arc(0, 0, r, 0, math.tau);
  }
};

exports.default = symbolCircle;
