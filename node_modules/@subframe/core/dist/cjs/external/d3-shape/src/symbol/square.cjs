'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var math = require('../math.cjs');

var symbolSquare = {
  draw(context, size) {
    const w = math.sqrt(size);
    const x = -w / 2;
    context.rect(x, x, w, w);
  }
};

exports.default = symbolSquare;
